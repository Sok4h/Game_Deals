package com.sok4h.game_deals.workers

import android.content.Context
import androidx.work.ListenableWorker
import androidx.work.WorkerFactory
import androidx.work.WorkerParameters
/*
import com.sok4h.game_deals.data.repositories.IGamesRepository

class DealWorkerFactory(private val repository: IGamesRepository) :WorkerFactory(){
    override fun createWorker(
        appContext: Context,
        workerClassName: String,
        workerParameters: WorkerParameters
    ): ListenableWorker {

        return DealWorker(appContext,workerParameters,repository)
    }


}*/
